{
  "properties": [
    {
      "name": "rate-limit.enabled",
      "type": "java.lang.Boolean",
      "description": "Global kill switch for the Redis-backed rate limiter (RateLimiterInterceptor). Set false to disable entirely, e.g. for local dev without Redis running.",
      "defaultValue": true
    },
    {
      "name": "rate-limit.default-tier.capacity",
      "type": "java.lang.Integer",
      "description": "Max tokens (requests) the default tier's bucket can hold. Applies to any endpoint not matched by sensitive-paths or write-paths — ordinary browsing/reading (search, view a listing, currency rates).",
      "defaultValue": 300
    },
    {
      "name": "rate-limit.default-tier.refill-tokens",
      "type": "java.lang.Integer",
      "description": "Tokens added back to the default tier's bucket every refill-period-seconds.",
      "defaultValue": 300
    },
    {
      "name": "rate-limit.default-tier.refill-period-seconds",
      "type": "java.lang.Integer",
      "description": "How often (in seconds) the default tier's bucket refills by refill-tokens.",
      "defaultValue": 60
    },
    {
      "name": "rate-limit.write-tier.capacity",
      "type": "java.lang.Integer",
      "description": "Max tokens the write tier's bucket can hold. Applies to endpoints listed in write-paths — creating/editing/deleting listings.",
      "defaultValue": 30
    },
    {
      "name": "rate-limit.write-tier.refill-tokens",
      "type": "java.lang.Integer",
      "description": "Tokens added back to the write tier's bucket every refill-period-seconds.",
      "defaultValue": 30
    },
    {
      "name": "rate-limit.write-tier.refill-period-seconds",
      "type": "java.lang.Integer",
      "description": "How often (in seconds) the write tier's bucket refills by refill-tokens.",
      "defaultValue": 60
    },
    {
      "name": "rate-limit.sensitive-tier.capacity",
      "type": "java.lang.Integer",
      "description": "Max tokens the sensitive tier's bucket can hold. Applies to endpoints listed in sensitive-paths — anything that moves money or generates a lead a human contacts (bookings, cancellations, promotions, expressing interest). Deliberately the strictest tier, since these are exactly the actions fraud/spam patterns target.",
      "defaultValue": 10
    },
    {
      "name": "rate-limit.sensitive-tier.refill-tokens",
      "type": "java.lang.Integer",
      "description": "Tokens added back to the sensitive tier's bucket every refill-period-seconds.",
      "defaultValue": 10
    },
    {
      "name": "rate-limit.sensitive-tier.refill-period-seconds",
      "type": "java.lang.Integer",
      "description": "How often (in seconds) the sensitive tier's bucket refills by refill-tokens.",
      "defaultValue": 60
    },
    {
      "name": "rate-limit.sensitive-paths",
      "type": "java.util.List<java.lang.String>",
      "description": "Endpoints checked against the sensitive tier, as 'METHOD:ant-pattern' entries (e.g. 'POST:/bookings/*/cancel'). Checked before write-paths, first match wins; unmatched requests fall into the default tier."
    },
    {
      "name": "rate-limit.write-paths",
      "type": "java.util.List<java.lang.String>",
      "description": "Endpoints checked against the write tier, as 'METHOD:ant-pattern' entries (e.g. 'PUT:/listings/*'). Checked after sensitive-paths; unmatched requests fall into the default tier."
    },
    {
      "name": "auth.service.url",
      "type": "java.lang.String",
      "description": "Base URL of the authentication service."
    },
    {
      "name": "wallet.service.url",
      "type": "java.lang.String",
      "description": "Base URL of the wallet service. All payment execution (M-Pesa, PayPal, Stripe, Flutterwave, NOWPayments) now lives there; this service only calls its internal debit endpoint.",
      "defaultValue": "http://localhost:8084"
    },
    {
      "name": "mail-from",
      "type": "java.lang.String",
      "description": "The 'From' address EmailService uses on every booking/interest notification email. Not a Spring Boot-recognized mail property — Spring's mail starter has no such setting, this is purely this service's own convention.",
      "defaultValue": "no-reply@premisave.com"
    },
    {
      "name": "internal.service.api-key",
      "type": "java.lang.String",
      "description": "Shared internal-service API key sent as X-API-Key on service-to-service calls (currently wallet-service's /internal/payment). Use the exact same value across every Premisave microservice — no default, since a wrong-but-present placeholder caused a confusing 401 at request time instead of a clear failure at startup."
    },
    {
      "name": "jwt.secret",
      "type": "java.lang.String",
      "description": "Base64-encoded secret key used to sign and verify JWT tokens. Must match across all services."
    },
    {
      "name": "cloudinary.cloud-name",
      "type": "java.lang.String",
      "description": "Cloudinary account cloud name."
    },
    {
      "name": "cloudinary.api-key",
      "type": "java.lang.String",
      "description": "Cloudinary API key."
    },
    {
      "name": "cloudinary.api-secret",
      "type": "java.lang.String",
      "description": "Cloudinary API secret."
    },
    {
      "name": "ad.promotion.daily-rate",
      "type": "java.math.BigDecimal",
      "description": "Daily rate charged for promoting an ad listing, in the default currency.",
      "defaultValue": 2.99
    },
    {
      "name": "ad.promotion.default-currency",
      "type": "java.lang.String",
      "description": "Default currency code used for ad promotion charges (e.g. USD).",
      "defaultValue": "USD"
    },
    {
      "name": "ad.promotion.expiry-check-cron",
      "type": "java.lang.String",
      "description": "Cron expression controlling how often AdPromotionService checks for and deactivates expired promotions. Standard 6-field Spring cron (seconds minutes hours day-of-month month day-of-week).",
      "defaultValue": "0 */5 * * * *"
    },
    {
      "name": "booking.completion-check-cron",
      "type": "java.lang.String",
      "description": "Cron expression controlling how often BookingService checks for CONFIRMED bookings past their checkOut date and marks them COMPLETED. Standard 6-field Spring cron.",
      "defaultValue": "0 */5 * * * *"
    },
    {
      "name": "frankfurter.base-url",
      "type": "java.lang.String",
      "description": "Base URL for the Frankfurter currency exchange rate API. Display-only conversion now — actual payment settlement happens in wallet-service.",
      "defaultValue": "https://api.frankfurter.dev"
    },
    {
      "name": "frontend.url",
      "type": "java.lang.String",
      "description": "Frontend application URL used as the default CORS allowed-origin when cors.allowed-origins is not set.",
      "defaultValue": "http://localhost:3000"
    },
    {
      "name": "cors.allowed-origins",
      "type": "java.lang.String",
      "description": "Comma-separated list of allowed CORS origins, e.g. 'https://app.premisave.com,https://admin.premisave.com'. Falls back to frontend.url for single-origin setups."
    }
  ],
  "hints": [
    {
      "name": "logging.level.values",
      "values": [
        {"value": "TRACE"},
        {"value": "DEBUG"},
        {"value": "INFO"},
        {"value": "WARN"},
        {"value": "ERROR"},
        {"value": "FATAL"},
        {"value": "OFF"}
      ]
    },
    {
      "name": "logging.level.keys",
      "values": [
        {"value": "root", "description": "Root logger level."},
        {"value": "com.premisave", "description": "All Premisave listing application classes."},
        {"value": "org.springframework.data.mongodb", "description": "MongoDB data layer."},
        {"value": "org.springframework.data.repository.config.RepositoryConfigurationExtensionSupport", "description": "Suppresses Redis/MongoDB repository conflict warnings."},
        {"value": "org.springframework.cloud.commons.util.InetUtils", "description": "Suppresses hostname resolution warnings from Spring Cloud."}
      ]
    }
  ]
}